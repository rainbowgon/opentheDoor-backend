server:
  servlet:
    context-path: /api

spring:
  h2:
    console:
      enabled: true

  datasource:
    url: jdbc:h2:tcp://localhost/~/opentheDoor
    username: sa
    password:
    driver-class-name: org.h2.Driver

  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        format_sql: true
        show_sql: true

logging:
  level:
    org.hibernate.sql: debug
    org.hibernate.type: trace

jwt:
  secret: dGhpc2lzb3BlbnRoZURvb3Jqd3RzZWNyZXRrZXk= # thisisopentheDoorjwtsecretkey BASE64 인코딩